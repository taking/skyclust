meta {
  name: Status
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/system/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
