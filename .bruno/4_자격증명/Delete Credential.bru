meta {
  name: Delete Credential
  type: http
  seq: 22
}

delete {
  url: {{baseUrl}}/api/{{apiVersion}}/credentials/{{credentialId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  
}

tests {
  test("Status code is 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
  
  test("Response is empty", function() {
    expect(res.getBody()).to.be.empty;
  });
}
