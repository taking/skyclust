meta {
  name: System Metrics
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/{{apiVersion}}/system/metrics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
